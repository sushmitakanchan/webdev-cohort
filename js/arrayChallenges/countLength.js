function countMovies(movieList) {
    // Return the number of movies in the movieList
    let numberOfMovies = movieList.length;
    return numberOfMovies;
}
 console.log(countMovies(["Inception", "Avatar"]));